# Runtime dependencies. Deliberately small — no LangChain, no vector DB,
# no web framework (plan Part 2).

feedparser>=6.0.11      # RSS/Atom parsing
httpx>=0.27             # HTTP for feeds and every LLM provider
pyyaml>=6.0             # config
pydantic>=2.7           # the stage-to-stage contract in models.py
scikit-learn>=1.4       # TF-IDF fallback clustering
numpy>=1.26
jinja2>=3.1             # static site templating
fastembed>=0.8          # ONNX MiniLM embeddings for clustering (CPU, ~130MB)
